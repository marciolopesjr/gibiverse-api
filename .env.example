# Porta da aplicação
PORT=3000

# Chave secreta para assinar os tokens JWT. Gere uma chave forte e aleatória.
# Exemplo: `openssl rand -base64 32`
JWT_SECRET=sua-chave-super-secreta-aqui

# Configurações da Aplicação
# ---------------------------

# Duração da expiração do token JWT (ex: '8h', '1d', '7d')
JWT_EXPIRES_IN=8h

# Custo do hashing da senha com bcrypt (salt rounds). Padrão é 10.
# Aumentar o valor torna o hash mais lento e seguro, mas consome mais CPU.
BCRYPT_SALT_ROUNDS=10

# Número padrão de itens por página nas listagens paginadas.
DEFAULT_PAGINATION_LIMIT=10

# Configuração do PostgreSQL (URL de conexão)
DATABASE_URL=postgres://user:password@localhost:5432/gibiverse